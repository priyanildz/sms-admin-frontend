// import React from "react";
// import { Navigate } from "react-router-dom";

// const PrivateRoute = ({ children }) => {
//   // Assuming you have some kind of authentication status (like a token or user state)
//   const isAuthenticated = localStorage.getItem("userToken"); // Replace with your authentication logic

//   if (!isAuthenticated) {
//     // Redirect to login page if not authenticated
//     return <Navigate to="/"/>;
//   }

//   return children; // If authenticated, render the children (protected page)
// };

// export default PrivateRoute;
